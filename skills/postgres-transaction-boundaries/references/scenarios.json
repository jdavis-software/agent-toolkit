{
  "schemaVersion": 1,
  "skill": "postgres-transaction-boundaries",
  "status": "not-run",
  "cases": [
    {
      "id": "postgres-transaction-boundaries-trigger",
      "kind": "trigger",
      "input": "An authorized task requires: Implement database invariants with explicit transaction ownership, isolation decisions, constraint-backed concurrency control, and retry-safe effects.",
      "expected": "Resolve actual inputs and versions, follow the domain procedure, and return implementation/review evidence with explicit limitations."
    },
    {
      "id": "postgres-transaction-boundaries-boundary",
      "kind": "boundary",
      "input": "An external effect completed before a transaction retry: prevent repeating it through durable coordination rather than claiming database atomicity covers the provider.",
      "expected": "Apply the stated boundary response, preserve unrelated work, and do not convert an unsupported or ambiguous condition into a pass."
    },
    {
      "id": "postgres-transaction-boundaries-non-trigger",
      "kind": "non-trigger",
      "input": "Correct a spelling mistake in an unrelated Markdown document.",
      "expected": "Make only the authorized small correction; do not activate this domain workflow or change tooling, credentials, services, or data."
    }
  ]
}
