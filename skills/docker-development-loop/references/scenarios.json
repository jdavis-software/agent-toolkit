{
  "schemaVersion": 1,
  "skill": "docker-development-loop",
  "status": "not-run",
  "cases": [
    {
      "id": "docker-development-loop-trigger",
      "kind": "trigger",
      "input": "An authorized task requires: Distinguish file synchronization, restart, recompilation, image rebuild, and platform validation in a container loop that preserves owned application state.",
      "expected": "Resolve actual inputs and versions, follow the domain procedure, and return implementation/review evidence with explicit limitations."
    },
    {
      "id": "docker-development-loop-boundary",
      "kind": "boundary",
      "input": "Two PostgreSQL containers share one writable data directory: reject that resource allocation instead of treating it as reusable cache.",
      "expected": "Apply the stated boundary response, preserve unrelated work, and do not convert an unsupported or ambiguous condition into a pass."
    },
    {
      "id": "docker-development-loop-non-trigger",
      "kind": "non-trigger",
      "input": "Correct a spelling mistake in an unrelated Markdown document.",
      "expected": "Make only the authorized small correction; do not activate this domain workflow or change tooling, credentials, services, or data."
    }
  ]
}
