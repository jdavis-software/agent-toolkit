{
  "schemaVersion": 1,
  "skill": "parallel-dispatch-planning",
  "status": "not-run",
  "cases": [
    {
      "id": "parallel-dispatch-planning-trigger",
      "kind": "trigger",
      "input": "Two independent adapters can run together, but the third implementation waits so review retains a slot. Their shared generated client has one upstream owner.",
      "expected": "Selected tasks and workers, resource/token reservations, deferred tasks with reasons, and launch-time checks."
    },
    {
      "id": "parallel-dispatch-planning-boundary",
      "kind": "boundary",
      "input": "An interrupted task may still be running on the same files",
      "expected": "An interrupted task may still be running on the same files: reconcile it before admitting a conflicting replacement."
    },
    {
      "id": "parallel-dispatch-planning-non-trigger",
      "kind": "non-trigger",
      "input": "Fix one spelling mistake in an unrelated README without changing any behavior.",
      "expected": "Apply the bounded edit; do not trigger an agent orchestration process or broaden execution authority."
    }
  ]
}
