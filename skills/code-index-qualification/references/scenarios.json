{
  "schemaVersion": 1,
  "skill": "code-index-qualification",
  "status": "not-run",
  "cases": [
    {
      "id": "code-index-qualification-trigger",
      "kind": "trigger",
      "input": "In the included synthetic corpus, a stored symbol answer initially matches worker.go. Changing that file without updating its expected hash makes the record check fail even if the supplied observation still claims fresh. A second fixture labels refresh as stale and cannot pass.",
      "expected": "Apply the named procedure to actual authorized inputs, preserve evidence scope and return the specified output without inventing completed operations."
    },
    {
      "id": "code-index-qualification-boundary",
      "kind": "boundary",
      "input": "A query succeeds using a stale index after a refresh lock timeout. Preserve the answer as exploratory evidence, but do not accept it as a current impact result.",
      "expected": "Expose the boundary as incomplete, invalid or blocked evidence; preserve work and do not manufacture a passing result."
    },
    {
      "id": "code-index-qualification-non-trigger",
      "kind": "non-trigger",
      "input": "Fix a spelling error in an unrelated documentation paragraph.",
      "expected": "Make the small authorized edit without enabling this workflow, installing services, reading private data or changing permissions."
    }
  ]
}
