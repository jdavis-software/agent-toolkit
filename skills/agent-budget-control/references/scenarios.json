{
  "schemaVersion": 1,
  "skill": "agent-budget-control",
  "status": "not-run",
  "cases": [
    {
      "id": "agent-budget-control-trigger",
      "kind": "trigger",
      "input": "A failed worker used more tokens than expected. Its usage stays charged, so the next implementation waits while verification retains its budget.",
      "expected": "Budget definitions, reservations, actual/unknown usage, remaining capacity, and a policy-bound next-action decision."
    },
    {
      "id": "agent-budget-control-boundary",
      "kind": "boundary",
      "input": "The provider exposes no usage count",
      "expected": "The provider exposes no usage count: mark it unknown and preserve a conservative charge, not zero."
    },
    {
      "id": "agent-budget-control-non-trigger",
      "kind": "non-trigger",
      "input": "Fix one spelling mistake in an unrelated README without changing any behavior.",
      "expected": "Apply the bounded edit; do not trigger an agent orchestration process or broaden execution authority."
    }
  ]
}
