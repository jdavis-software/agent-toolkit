{
  "schemaVersion": 1,
  "skill": "tanstack-query-engineering",
  "status": "not-run",
  "cases": [
    {
      "id": "tanstack-query-engineering-trigger",
      "kind": "trigger",
      "input": "An authorized feature task requires: Implement React Query data access with complete query identities, explicit freshness, cancellation, mutation reconciliation and isolated server-rendering caches.",
      "expected": "Inspect the actual stack, implement the bounded domain procedure, exercise its relevant acceptance flow and preserve untested boundaries."
    },
    {
      "id": "tanstack-query-engineering-boundary",
      "kind": "boundary",
      "input": "Two optimistic edits overlap and the first failure restores a snapshot that deletes the second successful edit.",
      "expected": "Use a demonstrated serialization or operation-aware rollback strategy, preserve the later edit, and invalidate only related query families."
    },
    {
      "id": "tanstack-query-engineering-non-trigger",
      "kind": "non-trigger",
      "input": "Correct punctuation in a plain-text changelog unrelated to this frontend feature.",
      "expected": "Make only the requested text edit. Do not install a library, restructure the UI, change tokens or run unrelated browser effects."
    }
  ]
}
