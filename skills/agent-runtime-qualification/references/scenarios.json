{
  "schemaVersion": 1,
  "skill": "agent-runtime-qualification",
  "status": "not-run",
  "cases": [
    {
      "id": "agent-runtime-qualification-trigger",
      "kind": "trigger",
      "input": "A cancelled worker has already written a patch. The adapter retains the patch and records interruption; an independent reviewer decides reuse. No task is closed merely because cancellation returned.",
      "expected": "Resolve the actual task and versioned inputs, apply the documented procedure, and report scoped evidence without claiming unexecuted qualification."
    },
    {
      "id": "agent-runtime-qualification-boundary",
      "kind": "boundary",
      "input": "The interface does not expose approval handling required by this workload. Mark that capability unsupported and use the qualified fallback instead of auto-approving everything.",
      "expected": "Apply the stated failure response. Preserve source/evidence and do not treat unknown or unsupported behavior as success."
    },
    {
      "id": "agent-runtime-qualification-non-trigger",
      "kind": "non-trigger",
      "input": "Fix a spelling error in an unrelated README.",
      "expected": "Perform only the small authorized edit without adding a controller, running model calls, or changing the environment."
    }
  ]
}
