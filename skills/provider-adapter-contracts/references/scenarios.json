{
  "schemaVersion": 1,
  "skill": "provider-adapter-contracts",
  "status": "not-run",
  "cases": [
    {
      "id": "provider-adapter-contracts-trigger",
      "kind": "trigger",
      "input": "An authorized task requires: Integrate providers through capability-aware contracts, normalized outcomes, bounded retries, authenticated callbacks, and explicit reconciliation/cancellation differences.",
      "expected": "Resolve actual inputs and versions, follow the domain procedure, and return implementation/review evidence with explicit limitations."
    },
    {
      "id": "provider-adapter-contracts-boundary",
      "kind": "boundary",
      "input": "Submission timed out before receiving the provider reference: reconcile if supported or report indeterminate rather than blindly submit again.",
      "expected": "Apply the stated boundary response, preserve unrelated work, and do not convert an unsupported or ambiguous condition into a pass."
    },
    {
      "id": "provider-adapter-contracts-non-trigger",
      "kind": "non-trigger",
      "input": "Correct a spelling mistake in an unrelated Markdown document.",
      "expected": "Make only the authorized small correction; do not activate this domain workflow or change tooling, credentials, services, or data."
    }
  ]
}
