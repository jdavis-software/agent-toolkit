{
  "schemaVersion": 1,
  "skill": "controller-conformance-testing",
  "status": "not-run",
  "cases": [
    {
      "id": "controller-conformance-testing-trigger",
      "kind": "trigger",
      "input": "The reference fixture passes seven cases. Seven deliberately defective variants each fail their named case. A real adapter must repeat those behaviors against the actual selected controller.",
      "expected": "Resolve the actual task and versioned inputs, apply the documented procedure, and report scoped evidence without claiming unexecuted qualification."
    },
    {
      "id": "controller-conformance-testing-boundary",
      "kind": "boundary",
      "input": "A controller ignores a failed retention hook and deletes the directory. Fail the actual retention case; adding more warnings to the hook is not a repair.",
      "expected": "Apply the stated failure response. Preserve source/evidence and do not treat unknown or unsupported behavior as success."
    },
    {
      "id": "controller-conformance-testing-non-trigger",
      "kind": "non-trigger",
      "input": "Fix a spelling error in an unrelated README.",
      "expected": "Perform only the small authorized edit without adding a controller, running model calls, or changing the environment."
    }
  ]
}
