{
  "schemaVersion": 1,
  "skill": "mcp-adapter-design",
  "status": "not-run",
  "cases": [
    {
      "id": "mcp-adapter-design-trigger",
      "kind": "trigger",
      "input": "An authorized task requires: Design thin MCP adapters with bounded schemas, trusted application identity, shared authorization, and explicit effects rather than treating instructions as permissions.",
      "expected": "Resolve actual inputs and versions, follow the domain procedure, and return implementation/review evidence with explicit limitations."
    },
    {
      "id": "mcp-adapter-design-boundary",
      "kind": "boundary",
      "input": "A client supplies another tenant\u2019s ID in valid JSON: enforce trusted identity and reject unauthorized access regardless of schema validity.",
      "expected": "Apply the stated boundary response, preserve unrelated work, and do not convert an unsupported or ambiguous condition into a pass."
    },
    {
      "id": "mcp-adapter-design-non-trigger",
      "kind": "non-trigger",
      "input": "Correct a spelling mistake in an unrelated Markdown document.",
      "expected": "Make only the authorized small correction; do not activate this domain workflow or change tooling, credentials, services, or data."
    }
  ]
}
