{
  "schemaVersion": 1,
  "skill": "generated-client-integrity",
  "status": "not-run",
  "cases": [
    {
      "id": "generated-client-integrity-trigger",
      "kind": "trigger",
      "input": "An authorized task requires: Keep generated Go and TypeScript clients, validators, and queries aligned with canonical inputs using pinned generation, reproducible output, and real consumer/wire tests.",
      "expected": "Resolve actual inputs and versions, follow the domain procedure, and return implementation/review evidence with explicit limitations."
    },
    {
      "id": "generated-client-integrity-boundary",
      "kind": "boundary",
      "input": "Consumed generated files are ignored by Git: prove downstream invalidation follows their content rather than relying on git diff alone.",
      "expected": "Apply the stated boundary response, preserve unrelated work, and do not convert an unsupported or ambiguous condition into a pass."
    },
    {
      "id": "generated-client-integrity-non-trigger",
      "kind": "non-trigger",
      "input": "Correct a spelling mistake in an unrelated Markdown document.",
      "expected": "Make only the authorized small correction; do not activate this domain workflow or change tooling, credentials, services, or data."
    }
  ]
}
