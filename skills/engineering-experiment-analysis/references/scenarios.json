{
  "schemaVersion": 1,
  "skill": "engineering-experiment-analysis",
  "status": "not-run",
  "cases": [
    {
      "id": "engineering-experiment-analysis-trigger",
      "kind": "trigger",
      "input": "Three synthetic tasks run under baseline and candidate conditions. One baseline task needs two attempts; one candidate never reaches acceptance. The report counts the failed candidate, adds both baseline attempts, and reports timing differences only for the two accepted complete pairs, explicitly not for the entire workload.",
      "expected": "Apply the named procedure to actual authorized inputs, preserve evidence scope and return the specified output without inventing completed operations."
    },
    {
      "id": "engineering-experiment-analysis-boundary",
      "kind": "boundary",
      "input": "All fast candidate runs succeeded, but failed candidate tasks were removed from the CSV. The fixed workload completeness check fails; do not report a favorable average over the remaining rows.",
      "expected": "Expose the boundary as incomplete, invalid or blocked evidence; preserve work and do not manufacture a passing result."
    },
    {
      "id": "engineering-experiment-analysis-non-trigger",
      "kind": "non-trigger",
      "input": "Fix a spelling error in an unrelated documentation paragraph.",
      "expected": "Make the small authorized edit without enabling this workflow, installing services, reading private data or changing permissions."
    }
  ]
}
