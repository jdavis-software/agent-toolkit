{
  "schemaVersion": 1,
  "skill": "temporal-worker-rollout",
  "status": "not-run",
  "cases": [
    {
      "id": "temporal-worker-rollout-trigger",
      "kind": "trigger",
      "input": "An authorized task requires: Qualify worker upgrades against self-hosted prerequisites, actual execution routing, replay evidence, external compatibility, and safe version drainage.",
      "expected": "Resolve actual inputs and versions, follow the domain procedure, and return implementation/review evidence with explicit limitations."
    },
    {
      "id": "temporal-worker-rollout-boundary",
      "kind": "boundary",
      "input": "An old pinned worker requires a database column removed by the rollout: reject the plan until that cross-system dependency is resolved.",
      "expected": "Apply the stated boundary response, preserve unrelated work, and do not convert an unsupported or ambiguous condition into a pass."
    },
    {
      "id": "temporal-worker-rollout-non-trigger",
      "kind": "non-trigger",
      "input": "Correct a spelling mistake in an unrelated Markdown document.",
      "expected": "Make only the authorized small correction; do not activate this domain workflow or change tooling, credentials, services, or data."
    }
  ]
}
