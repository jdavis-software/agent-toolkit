{
  "schemaVersion": 1,
  "skill": "agent-permission-delegation",
  "status": "not-run",
  "cases": [
    {
      "id": "agent-permission-delegation-trigger",
      "kind": "trigger",
      "input": "A parent can publish a repository, but its implementation child only receives local source and check access. Publication remains a separate approved operation.",
      "expected": "A least-privilege delegation binding, policy/identity sources, expiry/revocation rules, and executed denial evidence."
    },
    {
      "id": "agent-permission-delegation-boundary",
      "kind": "boundary",
      "input": "A child requests broader access than the parent holds",
      "expected": "A child requests broader access than the parent holds: deny and escalate through the legitimate owner, never self-authorize."
    },
    {
      "id": "agent-permission-delegation-non-trigger",
      "kind": "non-trigger",
      "input": "Fix one spelling mistake in an unrelated README without changing any behavior.",
      "expected": "Apply the bounded edit; do not trigger an agent orchestration process or broaden execution authority."
    }
  ]
}
