{
  "schemaVersion": 1,
  "skill": "typescript-type-design",
  "status": "not-run",
  "cases": [
    {
      "id": "typescript-type-design-trigger",
      "kind": "trigger",
      "input": "An authorized task requires: Design domain types and runtime input boundaries that distinguish legal states, optional values, and exhaustive transitions without unsafe assertions.",
      "expected": "Resolve actual inputs and versions, follow the domain procedure, and return implementation/review evidence with explicit limitations."
    },
    {
      "id": "typescript-type-design-boundary",
      "kind": "boundary",
      "input": "An API adds an enum value that an old client receives: use the documented unknown-value policy; local exhaustive types do not validate future wire values.",
      "expected": "Apply the stated boundary response, preserve unrelated work, and do not convert an unsupported or ambiguous condition into a pass."
    },
    {
      "id": "typescript-type-design-non-trigger",
      "kind": "non-trigger",
      "input": "Correct a spelling mistake in an unrelated Markdown document.",
      "expected": "Make only the authorized small correction; do not activate this domain workflow or change tooling, credentials, services, or data."
    }
  ]
}
