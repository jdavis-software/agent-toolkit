{
  "schemaVersion": 1,
  "skill": "tested-structural-refactoring",
  "status": "not-run",
  "cases": [
    {
      "id": "tested-structural-refactoring-trigger",
      "kind": "trigger",
      "input": "In an owned fixture with explicit debug/logger declarations, migrate a one-argument debug.trace call. Leave string/comment lookalikes, computed access and two-argument calls unchanged, and verify the transformed result and repeat application.",
      "expected": "Resolve the actual task and versioned inputs, apply the documented procedure, and report scoped evidence without claiming unexecuted qualification."
    },
    {
      "id": "tested-structural-refactoring-boundary",
      "kind": "boundary",
      "input": "The same syntactic function name is locally shadowed. Stop the broad rewrite and use semantic symbol resolution or narrow the accepted fixture scope; do not claim AST matching resolves identity.",
      "expected": "Apply the stated failure response. Preserve source/evidence and do not treat unknown or unsupported behavior as success."
    },
    {
      "id": "tested-structural-refactoring-non-trigger",
      "kind": "non-trigger",
      "input": "Fix a spelling error in an unrelated README.",
      "expected": "Perform only the small authorized edit without adding a controller, running model calls, or changing the environment."
    }
  ]
}
