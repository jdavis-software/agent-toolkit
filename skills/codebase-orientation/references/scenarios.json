{
  "schemaVersion": 1,
  "skill": "codebase-orientation",
  "status": "not-run",
  "cases": [
    {
      "id": "codebase-orientation-trigger",
      "kind": "trigger",
      "input": "A synthetic export has a TypeScript request mapper and a Go worker. The map observes each local entry point and labels the cross-process connection inferred until the message schema and publisher/consumer bindings are inspected. It does not draw a verified call edge merely because both files contain the word export.",
      "expected": "Apply the named procedure to actual authorized inputs, preserve evidence scope and return the specified output without inventing completed operations."
    },
    {
      "id": "codebase-orientation-boundary",
      "kind": "boundary",
      "input": "A graph has no edge to a generated client excluded from indexing. Report incomplete coverage and inspect the client directly; do not declare that there are no consumers.",
      "expected": "Expose the boundary as incomplete, invalid or blocked evidence; preserve work and do not manufacture a passing result."
    },
    {
      "id": "codebase-orientation-non-trigger",
      "kind": "non-trigger",
      "input": "Fix a spelling error in an unrelated documentation paragraph.",
      "expected": "Make the small authorized edit without enabling this workflow, installing services, reading private data or changing permissions."
    }
  ]
}
