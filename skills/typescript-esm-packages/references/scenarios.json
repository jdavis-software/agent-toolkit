{
  "schemaVersion": 1,
  "skill": "typescript-esm-packages",
  "status": "not-run",
  "cases": [
    {
      "id": "typescript-esm-packages-trigger",
      "kind": "trigger",
      "input": "An authorized task requires: Build ESM package boundaries against real Node/browser consumers, export maps, declaration files, and initialization behavior instead of workspace-only imports.",
      "expected": "Resolve actual inputs and versions, follow the domain procedure, and return implementation/review evidence with explicit limitations."
    },
    {
      "id": "typescript-esm-packages-boundary",
      "kind": "boundary",
      "input": "A downstream consumer requires CommonJS: record and test a deliberate compatibility route rather than claiming ESM is automatically sufficient.",
      "expected": "Apply the stated boundary response, preserve unrelated work, and do not convert an unsupported or ambiguous condition into a pass."
    },
    {
      "id": "typescript-esm-packages-non-trigger",
      "kind": "non-trigger",
      "input": "Correct a spelling mistake in an unrelated Markdown document.",
      "expected": "Make only the authorized small correction; do not activate this domain workflow or change tooling, credentials, services, or data."
    }
  ]
}
