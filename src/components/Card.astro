---
import { withBase } from '../lib/paths';
const { entry } = Astro.props;
const path = `${entry.kind === 'skill' ? 'skills' : entry.kind === 'tool' ? 'tools' : 'workflows'}/${entry.id}/`;
---
<article class="catalog-card" data-entry data-kind={entry.kind} data-origin={entry.origin} data-source={entry.sourceId ?? (entry.origin === 'original' ? 'jordan' : '')} data-category={entry.category} data-search={`${entry.title} ${entry.id} ${entry.description} ${entry.category} ${entry.author} ${entry.sourceName} ${entry.why}`.toLowerCase()}>
  <div class="card-top"><span class:list={['kind-icon',entry.kind]} aria-hidden="true">{entry.kind === 'skill' ? '◇' : entry.kind === 'tool' ? '⌘' : '⇄'}</span><span class="origin-label">{entry.origin === 'original' ? 'Original starter' : entry.origin === 'adapted' ? 'Adapted' : 'Community pick'}</span></div>
  <h3><a href={withBase(path)}>{entry.title}<span class="card-arrow" aria-hidden="true">↗</span></a></h3>
  <p class="card-author">{entry.sourceName}</p>
  <p>{entry.description}</p>
  <div class="card-bottom"><span>{entry.category}</span><span class:list={['status',{'experimental':entry.stage === 'experimental'}]}>{entry.vendorPath ? 'Source included' : entry.stage === 'reference' ? 'Upstream link' : entry.stage}</span></div>
</article>
